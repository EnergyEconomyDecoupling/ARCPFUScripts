This repository should be installed in the user's home folder on ARC.

Install with 

git clone https://github.com/EnergyEconomyDecoupling/ARCPFUScripts.git

Then, the following commands may be helpful:

* git checkout main
* chmod 755 <<script_name.sh>>
* git pull
* git push